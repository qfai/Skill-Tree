apiVersion: v1
kind: Service
metadata:
  name: testwork3
  namespace: namespace-workflow-1725439061500
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: testwork3
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080